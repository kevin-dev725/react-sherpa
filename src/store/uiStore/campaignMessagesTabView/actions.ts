import { createAction } from '../../../redux-helpers';
import {
  SET_CAMPAIGN_PROSPECT_FILTER
} from './actionTypes';

export const setCampaignProspectFilter = createAction(SET_CAMPAIGN_PROSPECT_FILTER);
